# Python dependencies for Parking Management System
# For deployment on UBC servers without PHP

# Core web framework
Flask==3.0.0
Werkzeug==3.0.1

# Database options
# For SQLite (already available on server)
# No additional requirements

# For Oracle connection (if network access allowed)
# oracledb==2.0.0

# For development/deployment
gunicorn==21.2.0
python-dotenv==1.0.0

# Optional: For better date handling
python-dateutil==2.8.2